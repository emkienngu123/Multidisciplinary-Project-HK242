dataset:
  anno_dir: ./data
  img_dir: ./data/FACEREG
  img_size: [480, 640]
  augmentation:
    flip:
      prob: 0.5
    rotation:
      degrees: 10
    color_jitter:
      brightness: 0.2
      contrast: 0.2
      saturation: 0.2
      hue: 0.1
model:
  embedding_size: 256